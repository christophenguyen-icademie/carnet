{% extends 'base.html.twig' %}
{%  block stylesheets %}
    <link href="/css/contact.css" rel="stylesheet">
{%  endblock %}
{% block body %}
    <div class="row">
        <div class="col-md-6 offset-md-3">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}

            <a href="{{ path('supprimecontact', { 'contact': codecontact}) }}" class="btn btn-danger">
                Archiver
            </a>
            <a href="{{ path('carnet_adresse') }}" class="btn btn-secondary">
                Retour au carnet
            </a>

        </div>
    </div>
{% endblock %}


{% block javascripts %}
    <script src="/js/contact.js"></script>
{% endblock %}
